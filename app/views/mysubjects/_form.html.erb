<%= form_for(@mysubject,  :remote => true, :format => :js, :position => :bottom, :update => "mysubject_index") do |f| %>
    <% if @mysubject.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@mysubject.errors.count, "error") %> prohibited this subject from being saved:</h2>

          <ul>
            <% @mysubject.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
    <div>
      <%=  f.label :name %>
      <%= f.text_field :name %>
    </div>


    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>